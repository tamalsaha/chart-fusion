apiVersion: v1
kind: Service
metadata:
  name: {{ include "mongodb-editor-options.fullname" . }}
  labels:
    {{- include "mongodb-editor-options.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mongodb-editor-options.selectorLabels" . | nindent 4 }}
