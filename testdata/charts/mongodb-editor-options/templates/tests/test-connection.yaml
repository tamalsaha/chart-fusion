apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mongodb-editor-options.fullname" . }}-test-connection"
  labels:
    {{- include "mongodb-editor-options.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mongodb-editor-options.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
