apiVersion: v1
kind: Service
metadata:
  name: {{ include "mongodb-editor.fullname" . }}
  labels:
    {{- include "mongodb-editor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mongodb-editor.selectorLabels" . | nindent 4 }}
